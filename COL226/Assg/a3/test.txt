// Test 1: Type compatibility between vectors
vector v1 := [1, 2, 3];
vector v2 := [4.5, 5.5, 6.5];  // Should work - numeric elements allowed
vector v3 := [1, 2];           // Different dimension for testing

// Test 2: Vector scalar operations
vector scaled_v1 := v1 * 2;      // Valid: vector * int
vector scaled_v2 := v2 * 3.14;   // Valid: vector * float

// Test 3: Vector-vector operations
vector v_sum := v1 + v2;         // Valid: same dimensions
float dot_prod := v1 . v2;       // Valid: dot product
float angle_val := angle(v1, v2); // Valid: angle between vectors
float mag_v1 := mag v1;          // Valid: magnitude
int dim_v1 := dim v1;            // Valid: dimension

// Test 4: Matrix operations
matrix m1 := [[1, 2], [3, 4]];
matrix m2 := [[5, 6], [7, 8]];
matrix m_sum := m1 + m2;         // Valid: matrix addition
matrix m_prod := m1 * m2;        // Valid: matrix multiplication
matrix m_trans := trans m1;      // Valid: transpose
float det_m1 := det m1;          // Valid: determinant

// Test 5: Matrix indexing
float elem := m1[0][1];          // Valid: matrix element access
m1[1][0] := 5;                   // Valid: matrix element update

// Test 6: Mixed type arithmetic
float mixed := 1 + 3.14;         // Valid: int + float
int i := 10;
float f := 3.14;

// Test 7: Control flow with vector conditions
if (mag v1 > 5.0) {
    print(v1);
} else {
    print(v2);
}

// Test 8: Loop with matrix operations
for (int i := 0; i < 2; i := i + 1) {
    m1[i][i] := m1[i][i] * 2;   // Valid: scaling diagonal elements
}

// Test 9: Vector assignments
vector v4 := v1;                 // Valid: vector assignment
v4[0] := 99;                     // Valid: vector element update



// Test 11: Invalid operations (commented out - should fail type checking)
// vector bad_vec := [true, false];     // Invalid: boolean elements
// vector mixed_vec := [1, "string"];   // Invalid: string elements
// matrix bad_mat := [[1,2], [3]];      // Invalid: inconsistent dimensions
// bool bad_op := v1 + true;            // Invalid: vector + boolean
// float bad_dot := v1 . m1;            // Invalid: dot product with matrix

// Test 12: Complex expressions
float complex_calc := mag(v1 + v2) - (mag v1 + mag v2);
print(complex_calc);

// Test 13: Matrix chaining
matrix m3 := trans(m1 * m2);     // Valid: composition of operations

// Test 14: Vector-Matrix dimension functions
int v_dim := dim v1;             // Valid: vector dimension
int m_rows := dim m1;            // Valid: matrix dimension

// Test 15: Special cases
matrix identity := [[1, 0], [0, 1]];
float det_id := det identity;     // Should be 1.0

// Test 16: Input/Output operations
int user_input := input("Enter a number: ");
print(user_input + 5);           // Valid: arithmetic with input
