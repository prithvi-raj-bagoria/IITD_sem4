// Test case for vector and matrix operations

// Vector declarations & operations
vector 3 v1;
v1 := 3 [1, 2, 3];
vector v2 := 3 [4, 5, 6];

// Vector operations 
vector 3 sum := v1 + v2;
vector 3 diff := v2 - v1;
float dot_product := v1 . v2;
float magnitude := mag v1;
int dimension := dim v2;
float angle_calc := angle(v1, v2);

// Matrix declarations & operations
matrix 2,2 m1;
m1 := 2,2 [[1, 2], [3, 4]];
matrix 2,2 m2 := 2,2 [[5, 6], [7, 8]];

// Matrix operations
matrix 2,2 mat_sum := m1 + m2;
matrix 2,2 mat_prod := m1 * m2;
matrix 2,2 transposed := trans m1;
float determinant := det m1;

// Accessing elements
int v_element := v1[1];
int m_element := m1[0][1];

print(dot_product);
print(determinant);
