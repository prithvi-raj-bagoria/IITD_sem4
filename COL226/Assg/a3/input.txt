// VECTOR AND MATRIX LANGUAGE TEST CASES

// -----------------------------
// 1. INPUT/OUTPUT COMMANDS
// -----------------------------
input        // Simple input command
print x      // Print variable
input()      // Input with empty parentheses 
input("matrix_data.txt")  // Input with filename
print("hello_world.txt")

// -----------------------------
// 2. VARIABLE DECLARATIONS
// -----------------------------
let b := true;          // Boolean variable
let i := 42;            // Integer variable
let s := 3.14;          // Scalar (float) variable
let v := [1, 2, 3];     // Vector variable
let m := [[1, 2], [3, 4]];  // Matrix variable

// -----------------------------
// 3. TYPE DECLARATIONS
// -----------------------------
bool isValid;
int count;
float scalar;
vector v3;
matrix m2x2;

// -----------------------------
// 4. OPERATIONS BY DATA TYPE
// -----------------------------
// 4.1 Boolean operations
let result := true && false;
let logicTest := !true || (false ^ true);

// 4.2 Integer operations
let sum := 5 + 3;
let product := 4 * 7;
let remainder := 10 % 3;
let comparison := 5 < 10;

// 4.3 Float operations
let pi := 3.14159;
let e := 2.71828;
let circleArea := pi * r * r;
let scientific := 6.022e23;

// 4.4 Vector operations
let v1 := [1, 2, 3];
let v2 := [4, 5, 6];
let vSum := v1 + v2;             // Vector addition
let vScaled := 2 * v1;           // Scalar multiplication
let dotProduct := v1 . v2;       // Dot product
let magnitude := abs v1;         // Vector magnitude

// 4.5 Matrix operations
let m1 := [[1, 2], [3, 4]];
let m2 := [[5, 6], [7, 8]];
let mSum := m1 + m2;             // Matrix addition
let mScaled := 2 * m1;           // Scalar multiplication
let mProduct := m1 * m2;         // Matrix multiplication
let mTranspose := transpose(m1); // Matrix transpose
let determinant := det(m1);      // Matrix determinant

// -----------------------------
// 5. CONTROL STRUCTURES
// -----------------------------
// 5.1 Assignment
x := x + 1;

// 5.2 Sequencing with blocks
{
    let x := 5;
    let y := 10;
    let z := x + y;
}

// 5.3 Conditionals
if (x > 5) {
    print "x is greater than 5";
} else if (x < 0) {
    print "x is negative";
} else {
    print "x is between 0 and 5";
}

// 5.4 For loop
for (let i := 0; i < 10; i := i + 1) {
    print i;
}

// 5.5 While loop
while (x < 100) {
    x := x * 2;
}

// -----------------------------
// 6. COMPLEX MATRIX ALGORITHMS
// -----------------------------
// 6.1 Matrix inversion (pseudocode)
let A := [[1, 2], [3, 4]];
let det := A[0][0] * A[1][1] - A[0][1] * A[1][0];
let invDet := 1 / det;
let inverse := [[A[1][1] * invDet, -A[0][1] * invDet], 
               [-A[1][0] * invDet, A[0][0] * invDet]];

// 6.2 Simple Gaussian elimination step (pseudocode)
let pivot := A[i][i];
for (let j := i + 1; j < n; j := j + 1) {
    let factor := A[j][i] / pivot;
    for (let k := i; k < n + 1; k := k + 1) {
        A[j][k] := A[j][k] - A[i][k] * factor;
    }
}

// -----------------------------
// 7. COMMENTS AND WHITESPACE
// -----------------------------
// This is a single-line comment

/* This is 
   a multi-line
   comment */

/* Nested /* comments */ should work */

// -----------------------------
// 8. ERROR CASES
// -----------------------------
123abc  // Invalid token (digit followed by letter)
"Unterminated string  // Missing closing quote
/* Unterminated comment  // Missing closing comment