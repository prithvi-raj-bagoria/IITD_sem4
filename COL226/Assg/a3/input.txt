// Testing comments
// Single line comment
/* Multi-line
   comment spanning
   multiple lines */
/* Nested /* comment */ works */

// Testing I/O commands
input()
input(test_file.txt)
input(data/matrix/m1.dat)
print(x'23)

// Testing type keywords and literals
bool flag := true;
bool another := false;
int count := 42;
float pi := 3.14159;

// Testing vector with dimension first (as per Piazza)
// Format: dimension followed by vector on next line
2
[1,2]

// Testing matrix with dimensions first (as per Piazza)
// Format: rows,cols followed by matrix on next line
2,3
[[1,2,3],[4,5,6]]

// Testing identifiers
a b c
x_1 y_2 variable_name
camelCase snake_case PascalCase
counter123

// Testing scientific notation for floats (as requested)
1.23e5
3.0e-2
1e3
.5e+10
1.e6

// Testing operators
// Arithmetic operators
a + b
c - d
x * y
total / count
remainder % divisor

// Comparison operators
a == b
c != d
x < y
min <= max
max >= threshold
score > passing

// Logical operators
condition1 and condition2
flag1 or flag2
not finished
bit1 xor bit2

// Special operators
abs(x)
dim v
mag vector1
trans matrix1
angle(v1, v2)
det m

// Testing assignment and control structures
x := 10;
if (x > 5) {
  y := x * 2;
} else {
  y := x / 2;
}

for (i := 0; i < 10; i := i + 1) {
  sum := sum + i;
}

while (count > 0) {
  count := count - 1;
}

do {
  val := val * 2;
} while (val < 100);

// Testing complex vector/matrix declarations and operations
// Vector declaration with dimension
3
[10, 20, 30]

v1 := 3
[1, 2, 3]

v2 := 3
[4, 5, 6]

// Matrix declaration with dimensions
2,2
[[1, 2], [3, 4]]

m1 := 2,2
[[1, 0], [0, 1]]

// Vector and matrix operations
dot := v1 . v2;
length := dim v1;
magnitude := mag v1;
transposed := trans m1;
theta := angle(v1, v2);
determinant := det m1;

// Testing mixed expressions
result := abs((x + y) * (z - w)) + (a and b) or (c < d);

// Testing delimiters
a, b, c;
{x; y; z}
(p * q)
[i, j, k]

// Testing handling of whitespace
a+b
c - d
x*     y
total/ count

// Testing float variations
0.5
.25
1.
123.456
1.23e+10
7.e-5

// Vector of integers and floats
3
[1, 2, 3]

3
[1.1, 2.2, 3.3]