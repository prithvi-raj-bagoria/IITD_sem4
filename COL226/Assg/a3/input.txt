// =============================================
// COMPREHENSIVE LEXICAL ANALYZER TEST SUITE
// Based on Piazza clarifications
// =============================================

// --- 1. I/O COMMANDS ---
input()                     // Input with no filename
input(data.txt)             // Input with simple filename
input(path/to/file.dat)     // Input with path
print(x)                    // Print identifier with parentheses

// --- 2. VARIABLE DECLARATIONS & IDENTIFIERS ---
// Valid identifiers (start with letter)
x y z counter matrix1 vector_3d result_matrix

// --- 3. TYPE DECLARATIONS ---
bool flag;
int count;
float pi;
vector v;
matrix m;

// --- 4. LITERALS ---
// Boolean literals
true false

// Integer literals - positive only
0 1 42 1000 123456789

// Float literals with proper scientific notation
0.0 1.0 3.14159 .5 1. 2.71828
1e10 2.5e3 1.5e-4 .5E+6 1.23e-10

// String literals
"Hello, world!"
""                      // Empty string
"Special chars: !@#$%^&*()"

// --- 5. BOOLEAN OPERATIONS ---
not flag
a and b
x or y
p xor q

// --- 6. ARITHMETIC OPERATIONS ---
a + b
c - d
e * f
g / h
i % j                   // Modulo
abs(x)                  // Absolute value

// --- 7. COMPARISON OPERATIONS ---
a == b                  // Equality
c != d                  // Inequality
e < f                   // Less than
g > h                   // Greater than
i <= j                  // Less than or equal
k >= l                  // Greater than or equal

// --- 8. VECTOR OPERATIONS ---
// Vector definition with dimension preceding (as specified in Piazza)
5
[1, 2, 3, 4, 5]               // Vector of size 5

// Vector operations
dot v1 v2              // Dot product
mag v1                  // Magnitude
dim v2                  // Dimension
angle v1 v2             // Angle between vectors

// Vector declaration in variable assignment (per Piazza)
vector v1 := 3
[1, 2, 3];

// --- 9. MATRIX OPERATIONS ---
// Matrix definition with dimensions preceding (as specified in Piazza)
2,3
[[1, 2, 3], [4, 5, 6]]  // 2x3 matrix

// Matrix operations
trans m1                // Transpose
det m2                  // Determinant

// Matrix declaration in variable assignment
matrix m1 := 2,2
[[1, 2], [3, 4]];

// --- 10. CONTROL STRUCTURES ---
// Assignment
x := 5;
y := a + b;

// If statements
if (x > 0) {
    y := x * 2;
}

if (a < b) {
    c := a + b;
} else {
    c := a - b;
}

// For loops
for (i := 0; i < 10; i := i + 1) {
    sum := sum + i;
}

// While loops
while (x > 0) {
    x := x - 1;
}

do {
    y := y * 2;
} while (y < 100);

// --- 11. COMMENTS ---
// This is a single line comment

/* This is a 
   multi-line comment */

/* This is a /* nested */ comment */

// --- 12. COMPLEX PROGRAMS ---
// Matrix determinant calculation
det_val := det(A);
if (det_val != 0) {
    print(inv);
} else {
    print(x);  // Print only handles identifiers
}

// --- 13. EDGE CASES ---
// Scientific notation edge cases
1.0e10
1e-5
.5e+10

// --- 14. VECTOR AND MATRIX ARITHMETIC ---
// Vector addition and scalar multiplication
3
[1, 2, 3]
3
[4, 5, 6]
// The addition would be represented as:
v1 + v2

// Matrix multiplication
2,2
[[1, 2], [3, 4]]
2,1
[[5], [6]]
// The multiplication would be represented as:
m1 * m2
